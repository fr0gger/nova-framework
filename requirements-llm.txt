# Nova Framework LLM Requirements
# Includes all dependencies for semantic matching and LLM evaluation
# Using ~= (compatible release) to allow patch updates but prevent breaking changes

# Include basic requirements
-r requirements-basic.txt

# LLM and semantic matching dependencies
sentence-transformers~=2.3.1
transformers~=4.36.2
openai~=1.12.0
anthropic~=0.18.1